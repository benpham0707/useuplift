---
phase: 01-routing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/DashboardHome.tsx
  - src/App.tsx
  - src/components/dashboard/AppSidebar.tsx
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - NAV-01

must_haves:
  truths:
    - "User lands on Dashboard Home page automatically after Clerk login"
    - "Dashboard Home displays at /dashboard route without errors"
    - "Home navigation item appears as first sidebar entry with home icon"
    - "User can navigate from /dashboard to /dashboard/scanner and back"
    - "Old routes like /portfolio-scanner redirect to new /dashboard/scanner location"
  artifacts:
    - path: "src/pages/DashboardHome.tsx"
      provides: "Dashboard Home page component with welcome banner and skeleton loading"
      min_lines: 60
    - path: "src/App.tsx"
      provides: "Nested route structure with index route and redirects"
      contains: "Route index element={<DashboardHome"
    - path: "src/components/dashboard/AppSidebar.tsx"
      provides: "Updated navigation with Home as first item"
      exports: ["AppSidebar"]
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/DashboardHome.tsx"
      via: "React Router index route"
      pattern: "Route index element=\\{<DashboardHome"
    - from: "src/components/dashboard/AppSidebar.tsx"
      to: "/dashboard"
      via: "Link component"
      pattern: "href: '/dashboard'"
---

<objective>
Make Dashboard Home accessible as the default landing page for authenticated users at /dashboard route. Restructure routes from flat structure to nested under /dashboard, add Home navigation item, implement backward-compatible redirects, and create minimal Dashboard Home page with welcome banner and loading skeleton.

Purpose: Establish routing foundation so Phase 2 can build dashboard UI components on top of working navigation infrastructure.

Output: Dashboard Home accessible, all features navigable under /dashboard/*, old routes redirect seamlessly.
</objective>

<execution_context>
@/Users/beenpam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beenpam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-routing-foundation/01-CONTEXT.md
@.planning/phases/01-routing-foundation/01-RESEARCH.md
@src/App.tsx
@src/layouts/DashboardLayout.tsx
@src/components/dashboard/AppSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dashboard Home page with skeleton and welcome banner</name>
  <files>src/pages/DashboardHome.tsx</files>
  <action>
Create new file src/pages/DashboardHome.tsx with:
- Import Home icon from lucide-react, X from lucide-react, Skeleton from @/components/ui/skeleton
- Export default DashboardHome component
- useState for loading state (initial true) and simulated useEffect that sets loading false after 500ms (simulates data fetch, will be replaced in Phase 3)
- If loading, render DashboardHomeSkeleton component
- Otherwise render main content with WelcomeBanner at top
- Main content: h1 "Dashboard Home", p explaining this is where next steps will appear
- Apply padding p-6, spacing space-y-6 for consistent layout
- DashboardHomeSkeleton: Skeleton for banner (h-16 w-full), title (h-8 w-64), subtitle (h-4 w-96)
- WelcomeBanner: Check localStorage 'dashboard-welcome-dismissed', if true return null
- Banner UI: bg-primary/10 border-l-4 border-primary p-4 rounded-r-md
- Banner text: "Welcome to your new dashboard! Find everything you need in one place."
- Dismiss button: X icon, onClick sets localStorage and state, aria-label "Dismiss welcome message"
- Use TypeScript strict mode, no any types
- Follow shadcn/ui styling patterns (existing Skeleton component works as-is, no custom shimmer needed for Phase 1)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Navigate to http://localhost:5173/dashboard (after route changes in Task 2) and verify:
1. Skeleton shows briefly during initial load
2. Welcome banner appears with dismissible X button
3. Clicking X removes banner permanently (persists across page refresh)
4. Page title "Dashboard Home" displays correctly
5. No console errors
    </manual>
    <sampling_rate>after task commit, before next task</sampling_rate>
  </verify>
  <done>DashboardHome.tsx exists, compiles without errors, renders welcome banner and placeholder content, skeleton loading works correctly</done>
</task>

<task type="auto">
  <name>Task 2: Restructure App.tsx routes to nested /dashboard structure</name>
  <files>src/App.tsx</files>
  <action>
Modify src/App.tsx route configuration (lines 74-111):

1. Import DashboardHome: Add `import DashboardHome from "./pages/DashboardHome";` after other page imports

2. Update authenticated route wrapper (currently line 100) to use "dashboard" path prefix:
   Change `<Route element={<RequireVerified><RequireTermsAccepted><DashboardLayout /></RequireTermsAccepted></RequireVerified>}>`
   to include "dashboard" in path attribute

3. Add index route as FIRST child under DashboardLayout wrapper:
   `<Route index element={<DashboardHome />} />`
   This renders at /dashboard exactly (not /dashboard/index)

4. Update ALL existing authenticated routes to use "dashboard/*" paths:
   - /portfolio-scanner → dashboard/scanner
   - /portfolio-insights → dashboard/insights
   - /piq-workshop → dashboard/workshop
   - /piq-workshop/:piqNumber → dashboard/workshop/:piqNumber
   - /pricing → dashboard/pricing
   - /settings → dashboard/settings

5. After closing DashboardLayout Route, add backward compatibility redirects BEFORE the "*" NotFound route:
   ```tsx
   {/* Backward compatibility redirects */}
   <Route path="/portfolio-scanner" element={<Navigate replace to="/dashboard/scanner" />} />
   <Route path="/portfolio-insights" element={<Navigate replace to="/dashboard/insights" />} />
   <Route path="/piq-workshop" element={<Navigate replace to="/dashboard/workshop" />} />
   <Route path="/piq-workshop/:piqNumber" element={<Navigate replace to="/dashboard/workshop/:piqNumber" />} />
   <Route path="/pricing" element={<Navigate replace to="/dashboard/pricing" />} />
   <Route path="/settings" element={<Navigate replace to="/dashboard/settings" />} />
   ```
   Import Navigate if not already: `import { BrowserRouter, Routes, Route, Navigate } from "react-router-dom";`

6. Verify "*" NotFound route remains LAST (critical for route matching order)

Why Navigate replace: Prevents redirect loops in browser history — back button won't hit old route and redirect again.

Verify DashboardLayout.tsx already has `<Outlet />` on line 13 (it does) — this is what renders child/index routes.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Test route navigation:
1. Visit http://localhost:5173/dashboard → shows Dashboard Home
2. Click Scanner in sidebar → URL changes to /dashboard/scanner, Scanner page loads
3. Browser back button → returns to /dashboard, Dashboard Home shows
4. Manually visit http://localhost:5173/portfolio-scanner → redirects to /dashboard/scanner
5. Visit http://localhost:5173/piq-workshop/1 → redirects to /dashboard/workshop/1
6. Verify sidebar stays mounted during navigation (no flicker/remount)
7. Verify unauthenticated user visiting /dashboard redirects to /auth (Clerk RequireVerified)
    </manual>
    <sampling_rate>after task commit, before next task</sampling_rate>
  </verify>
  <done>Routes restructured under /dashboard, index route renders Dashboard Home, redirects work for all old routes, authenticated guard functions correctly, layout persists across navigation</done>
</task>

<task type="auto">
  <name>Task 3: Update AppSidebar with Home navigation item and new route paths</name>
  <files>src/components/dashboard/AppSidebar.tsx</files>
  <action>
Modify src/components/dashboard/AppSidebar.tsx:

1. Import Home icon: Add `Home,` to lucide-react import list (line 14-22)

2. Update navItems array (line 29-55) with:
   - ADD Home as FIRST item:
     ```tsx
     {
       title: 'Home',
       href: '/dashboard',
       icon: Home,
     },
     ```
   - UPDATE all existing item hrefs to new /dashboard/* paths:
     - Scanner: '/portfolio-scanner' → '/dashboard/scanner'
     - Insights: '/portfolio-insights' → '/dashboard/insights'
     - Workshop: '/piq-workshop' → '/dashboard/workshop'
     - Pricing: '/pricing' → '/dashboard/pricing'
     - Settings: '/settings' → '/dashboard/settings'

3. Update isActive function (line 86-91) for exact match on Home:
   ```tsx
   const isActive = (href: string) => {
     // Exact match for home to avoid matching all /dashboard/* routes
     if (href === '/dashboard') {
       return location.pathname === '/dashboard';
     }
     // Existing logic for workshop (handles :piqNumber param)
     if (href === '/dashboard/workshop') {
       return location.pathname.startsWith('/dashboard/workshop');
     }
     // Exact match for all other routes
     return location.pathname === href;
   };
   ```
   This prevents Home from showing active when on /dashboard/scanner (which also starts with /dashboard).

Maintain all existing code structure, styling, credits display, user footer — only modify navItems array and isActive function.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Check sidebar navigation:
1. "Home" appears as first item in sidebar with house icon
2. Clicking "Home" navigates to /dashboard
3. Home item shows active state (highlighted) only when on /dashboard exactly
4. Scanner item shows active when on /dashboard/scanner (not when on /dashboard)
5. All other navigation items work correctly
6. Credits display and footer remain unchanged
7. Active state highlighting matches existing style (no visual regression)
    </manual>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>AppSidebar shows Home as first navigation item, all routes updated to /dashboard/* paths, active state detection works correctly for index and child routes, visual consistency maintained</done>
</task>

</tasks>

<verification>
## End-to-End Verification

After all tasks complete, verify complete phase requirements:

**ROUT-01 - /dashboard shows Dashboard Home:**
- Visit /dashboard while authenticated → Dashboard Home page displays
- No errors in console, no infinite redirects

**ROUT-02 - All existing features accessible:**
- /dashboard/scanner → PortfolioScanner loads
- /dashboard/insights → PortfolioInsightsNew loads
- /dashboard/workshop → PIQWorkshop loads
- /dashboard/workshop/1 → PIQWorkshop with piqNumber param loads
- /dashboard/pricing → Pricing loads
- /dashboard/settings → Settings loads

**ROUT-03 - Auth integration:**
- Unauthenticated user visiting /dashboard → redirects to /auth
- After login → lands on /dashboard automatically (Clerk default post-login)

**NAV-01 - Home in sidebar:**
- "Home" appears as first sidebar item
- House icon displays correctly
- Clicking navigates to /dashboard

**Backward compatibility:**
- Old bookmarks (/portfolio-scanner, /piq-workshop, etc.) redirect to new paths
- No 404 errors for old routes

**Layout persistence:**
- Navigating between dashboard pages doesn't remount sidebar
- Credits display persists
- No visual flicker during navigation
</verification>

<success_criteria>
Phase 1 complete when:
- [ ] Dashboard Home page exists and renders without errors
- [ ] /dashboard route displays Dashboard Home as default authenticated landing
- [ ] All features accessible under /dashboard/* routes
- [ ] Old routes redirect seamlessly to new locations
- [ ] "Home" navigation item appears first in sidebar with home icon
- [ ] Active state highlighting works correctly (Home vs child routes)
- [ ] Sidebar layout persists across navigation (no remount)
- [ ] Welcome banner displays and can be dismissed permanently
- [ ] Unauthenticated users redirected to /auth
- [ ] No TypeScript errors (npx tsc --noEmit passes)
- [ ] All route transitions work smoothly without layout shift
</success_criteria>

<output>
After completion, create `.planning/phases/01-routing-foundation/01-01-SUMMARY.md` documenting:
- Files created/modified
- Route structure changes (flat → nested)
- Navigation updates
- Verification results
- Any issues encountered and resolutions
</output>
